__pycache__/
*.py[cod]
.venv/
.env
.mypy_cache/
.pytest_cache/
.ruff_cache/
node_modules/
dist/
.DS_Store

# Reviewer-uploaded images (settings.media_root). Local disk is the store
# while deployment is deferred; published articles point into it, so back it
# up with the database rather than treating it as scratch.
backend/var/
notesForME.txt
